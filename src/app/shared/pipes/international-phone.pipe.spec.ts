import { InternationalPhonePipe } from './international-phone.pipe';

describe('InternationalPhonePipe', () => {
  it('create an instance', () => {
    const pipe = new InternationalPhonePipe();
    expect(pipe).toBeTruthy();
  });
});
